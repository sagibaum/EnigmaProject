package parts.reflector;

public interface ReflectorInterface {

    public Integer returnLocationIndex(Integer indexOfInputChar);
    //reflector needs the ability to receive location index, and output relevant index based on XML file
}
